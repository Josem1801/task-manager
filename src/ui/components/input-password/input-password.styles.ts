import styled from "styled-components";

export const ToggleButton = styled.button`
  position: absolute;
  right: 1rem;
  top: 50%;
  width: 1.5rem;
  height: 1.5rem;
  margin: auto 0;
  transform: translateY(-50%);
  background: none;
  border: none;
  cursor: pointer;
`;
